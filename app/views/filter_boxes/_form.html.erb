<%= form_for(@filter_box) do |f| %>
  <% if @filter_box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@filter_box.errors.count, "error") %> prohibited this filter_box from being saved:</h2>

      <ul>
      <% @filter_box.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
