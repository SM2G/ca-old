<h1>Personnel</h1>

<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Statut</th>
      <th>Nom</th>
      <th>Status</th>
      <th>Profil</th>
      <th colspan="6"></th>
    </tr>
  </thead>
  <tbody>
    <% @employees.order(is_active: :desc).each do |employee| %>
      <tr class='<%= "is_active" if employee.is_active? %>'>
        <td>
        <p class="<%= employee.is_active ? "text-success" : "text-danger"%>">
          <span class="<%= employee.is_active ? "glyphicon glyphicon-ok" : "glyphicon glyphicon-remove"%> aria-hidden=true>"</span></td>
        </p>

        <td><%= employee.first_name %> <%= employee.last_name %></td>
        <td><%= employee.status %></td>
        <td><%= employee.profile.profile_name %></td>
        <td>
        <div class="btn-group">
          <%= link_to "Modifier", edit_employee_path(employee), :class => "btn btn-default" %></a>
          <a aria-expanded="false" href="#" class="btn btn-default dropdown-toggle" data-toggle="dropdown"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Détails', employee %></li>
            <!--<li><%= link_to 'Modifier', edit_employee_path(employee) %></li>-->
            <li class="divider"></li>
            <li><%= link_to "Supprimer", employee, method: :delete, data: { confirm: 'Are you sure?' }, :class => "text-danger" %></li>
          </ul>
          <%= link_to "Supprimer", employee, method: :delete, data: { confirm: 'Are you sure?' }, :class => "text-danger" %>
        </div>
        <!--<td><%= link_to 'Détails', employee , :class => "btn btn-default" %></td>-->
        <!--<td><%= link_to 'Modifier', edit_employee_path(employee), :class => "btn btn-default" %></td>-->
        <!--<td><%= link_to 'Supprimer', employee, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %>-->
      </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvel Employé', new_employee_path, :class=>"btn btn-primary" %>
